# copy to .env and fill in — .env is gitignored, never commit real keys
ANTHROPIC_API_KEY=

# x / twitter api (developer.x.com — free tier is fine to start)
X_API_KEY=
X_API_SECRET=
X_ACCESS_TOKEN=
X_ACCESS_TOKEN_SECRET=

# wallet (solana) — leave empty until the wallet skill is built
WALLET_PRIVATE_KEY=
WALLET_SPEND_CAP_USD=10
